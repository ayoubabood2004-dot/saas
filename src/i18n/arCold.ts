/* النصفُ البارد من القاموس العربيّ — نصوصُ الشاشات الكسولة.
 *
 * تُحمَّل مرّةً واحدةً بـ`ensureDictionary()` (`index.ts`)، وكلُّ صفحةٍ تنتظرها
 * (`page()` بـ`src/lib/lazyPage.ts`) فلا يُرسم مفتاحٌ خامٌ ولا نصٌّ افتراضيٌّ
 * إنكليزيٌّ على شاشةٍ عربية.
 *
 * وكأختها `arHot.ts` **تعني شيئين**:
 *   • داخل Vite يستبدل `i18nSplit()` محتواها بالنصف البارد وحدَه — حزمةٌ واحدةٌ
 *     `arCold-*.js` لا يُعلنها `index.html` ويخبّئها العاملُ الخدميّ للعمل بلا إنترنت؛
 *   • وخارجه هي القاموسُ كاملاً (أكثرُ من اللازم، لا أقلّ).
 *
 * ولا يستوردها إلا `index.ts`، وبـ`import()` ديناميكيٍّ وحدَه — استيرادٌ ثابتٌ
 * يعيدها لمسار الإقلاع ويُسقط البناء. */
import ar from "./ar.json";

export default ar;
