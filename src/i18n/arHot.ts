/* النصفُ الحارّ من القاموس العربيّ — ما ترسم به القشرةُ قبل أوّل صفحة.
 *
 * هذه الوحدةُ **تعني شيئين** وهذا مقصود:
 *   • داخل Vite (البناءُ وخادمُ التطوير) يستبدل مُلحَقُ `i18nSplit()`
 *     (`scripts/i18n-split.mjs`) محتواها كلَّه بالنصف الحارّ وحدَه — المساراتُ
 *     المسمّاة بـ`hot-paths.json`.
 *   • وخارجه (tsc، وحُزمُ esbuild بالفحوص، و`lang-default-test`) هي القاموسُ
 *     كاملاً كما تراه أدناه — فالفحوصُ ترى مفاتيحَ أكثرَ من اللازم، لا أقلّ.
 *
 * لا تستوردها من غير `index.ts`، ولا تستورد `ar.json` مباشرةً من أيّ مكان:
 * يعود القاموسُ كلُّه لمسار الإقلاع (يمسكه `i18n-hot-guard` وتأكيداتُ البناء). */
import ar from "./ar.json";

export default ar;
