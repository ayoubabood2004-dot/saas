# ملفّاتٌ مولَّدةٌ يقارنها حارسٌ بايتاً ببايت بما يولّده — نهاياتُ أسطرها LF بأيّ سحب.
#
# `core.autocrlf=true` (افتراضُ ويندوز) يحوّل LF إلى CRLF عند السحب، فكلُّ worktree
# جديدٍ على جهاز ويندوز كان يفشل `store-i18n --check` قبل أن يلمسه أحد — والمحتوى
# مطابقٌ حرفياً — ويقف `lint` عنده فلا يجري ما بعده من حرّاس. (Vercel لينكس: سليم.)
src/i18n/store.ar.json text eol=lf
