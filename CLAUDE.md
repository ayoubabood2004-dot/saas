# doctorVet — ما يحتاجه كلُّ من يشتغل هنا

نظامُ عياداتٍ بيطرية عربيّ (لهجة عراقية)، يعمل على **عيادات حقيقية ببيانات
حقيقية**. هذا الملفّ يُقرأ ببداية كل جلسة، فما فيه إلا ما يوفّر خطأً أو ساعة.

---

## ١) خطوطٌ لا تُتجاوَز

- **لا هجومَ سيبرانيّ حيّ** على `doctorvet.vet` — بيانات عيادات حقيقية.
- **`SUPABASE_SERVICE_ROLE_KEY` للخادم وحده.** لا بادئة `VITE_` أبداً — كلُّ ما
  بادئتُه `VITE_` يُحزَم بالمتصفّح ويصير علنياً.
- **لا تعطيل TLS** ولا لمسَ `HTTPS_PROXY`.
- **المستودع عام.** لا وثائقَ خاصة (`private/` متجاهَل)، ولا معرّفاتِ نماذجَ
  بالدفعات ولا التعليقات، ولا أسرارَ بأي مخرَجٍ محفوظ.
- **التطوير على `claude/saas-project-b2hfi6`.** الدمجُ إلى `main` ينشر على
  عياداتٍ حيّة — **بكلمة المالك وحدها**، ولا يُفتح طلبُ دمجٍ بلا طلب صريح.

---

## ٢) الفحوص — تُشغَّل قبل كل دفعة

```bash
npm run lint     # tsc + i18n-guard + db-guard + outbox + errors + payroll + search
npm run build    # نفسها + البناء الفعلي
bash supabase/tests/run.sh   # الهجرات على بوستغريس حقيقيّ (يحتاج PGBIN)
```

الحرّاس تمسك أشياءَ حقيقية، وقد أمسكت فعلاً: مفتاحاً أجنبياً بلا فهرس، ونداءً
عارياً بسياسة RLS، ومفاتيحَ ترجمةٍ غير متكافئة. **لا تُسكِت حارساً — أصلح.**

- **i18n-guard**: كلُّ عربيةٍ معروضة تمرّ من `t(`. مفاتيح `en.json`/`ar.json`
  متطابقة (الإنكليزية هي الأساس). لكل ملفٍّ سقفُ نصٍّ صلب **ينزل ولا يصعد**؛
  و`Payroll.tsx` و`payrollDemo.ts` سقفُهما **صفر**.
- **db-guard**: كلُّ `security definer` يحتاج `set search_path`. ولا
  `numeric(p,s)`. والأساس: فهارس مكرّرة ٤، وrls-initplan ٤٣.

---

## ٣) مصائدُ وقعنا بيها — لا تقع بيها مرّتين

**plpgsql**: `IF` يُقطع عند **أوّل `THEN` غير مقوَّس** — ومنه `THEN` الـ`CASE`.
```sql
if v.kind <> (case v.code when 'LOAN' then 'loan' else 'advance' end) then  -- الأقواس لازمة
```

**PostgREST**: `.maybeSingle()` **يرمي خطأً** على أكثر من صفّ. وسقفُ الصفوف
لكل طلب = ألف (مقيسٌ على الإنتاج)، فـ`allPages` تتقدّم **بما وصل** لا بما طُلب،
وتتوقّف عند صفحةٍ **فارغة** لا ناقصة.

**قائمةٌ ناقصة أخطرُ من خطأ ظاهر.** الخطأ يُرى ويُعاد؛ النقصُ يُصدَّق. عيادةٌ
رأت «ماكو منتج» عن مادةٍ برفّها فأعادت إدخالها. فلا تُرجع قائمةً جزئية عند
الفشل — ارمِ الخطأ واعرض «أعد المحاولة».

**Tailwind**: السلالم الدلالية تقف عند **700**. موجود: `text-2xs`،
`bg-surface-1/2/3`، `border-line-strong`، والمنطقيّ `start-*`/`end-*`.

**المنتج له رمزٌ واحد بالنظام وعدّة رموز بالواقع.** رقمُ الرفّ الذي كُتب أوّل
يوم، وباركودُ المصنع على العلبة. فيُدخَل برمزٍ ويُمسَح بآخر ويقول النظام «لا
يوجد» — والمنتج موجود. المقيس: ٢٨١ منتجاً بأربع عيادات رمزُه يدويّ. الدفاعُ
على طرفين: عند المسح اربطِ الرمزَ بالقائم (`attach_product_code`)، وعند
الإدخال افحص مخزنَ العيادة **بالتطبيع على الطرفين** (`findByCode`) قبل أن
تسمح بتوأم. والتوائمُ القائمة تُطوى بـ`merge_products` لا تُحذف.

**«اختفى كأنه ما كان» = حُذف.** عيادتان أبلغتا عن مادةٍ مباعةٍ اختفت؛ سجلُّ
التدقيق قال: زرُّ الحذف على الحساب المشترك، بتأكيدِ `window.confirm` يُقبل بلا
قراءة. فالحذفُ صار طيّاً: `delete_product` يحفظ الصفَّ ومعرّفاتِ سطوره في
`products_trash` و`restore_product` يعيده **بنفس المعرّف** (0145). ومنذ 0146
**محفّزٌ قبل الحذف** على `products` يصوّر أيَّ صفٍّ يخرج بأي طريق (دمج، ترتيب،
حذفٌ مباشر من نسخةٍ قديمة)، والدمجُ يُفكّ من تبويب المحذوفات. لا تؤكّد حذفاً
بنافذة متصفّح — قل للمستخدم ماذا يحذف.

**دالّةٌ تكتب بجدولٍ سياسته قراءةٌ فقط لازم `security definer` + فحصُ
العيادة والدور بنفسها.** 0145 نزلت بصلاحية المُستدعي فكان أوّلُ حذفٍ سيُرفض
بـRLS. الحزمة تجري كـsuperuser فلا تمسك هذا — فاكتب فحصاً ثابتاً (prosecdef +
`clinic_id = v_clinic` بالنصّ) مع كل دالّةٍ كهذه.

**«اختفى» بمعنى «رصيده صفر».** فاتورةٌ واحدة باعت رفّاً كاملاً (اكتب ٤٧ وامسح)
فصار المنتج رمادياً بشاشة البيع والمسحُ صامتاً. المسحُ على رصيدٍ صفر يقولها
بصوت، وبيعُ الرصيد كلّه بكميةٍ ≥ ١٠ يُؤكَّد بالاسم والعدد.

**القاعدةُ تجمع والمتصفّحُ يعرض (0149).** لا قراءةَ لجداول المال بلا مدّة:
`reports-guard` يفشّل `listInvoices(`/`listAllInvoiceItems(` بلا `{from,to}` ما لم
تُوسَم `/* unbounded: السبب */`. المجاميعُ من دوالّ `report_*`، ودفترُ الزبون
من `customer_invoices`. وكلُّ تغييرٍ بمنطق المال يمرّ بفحص التطابق
(`report-fixture` + `report-parity` داخل الحزمة): SQL مقابل `receiptsOf` فلساً
بفلس. الخطة الكاملة بـ`docs/reports-plan.md`.

**العربية والأرقام**: `searchable()` للبحث بالاسم (تطبيع همزة/ة/ى + أرقام)،
و`normalizeCode()` للباركود (يشيل المحارف غير المرئية والمسافات). **الطرفان**
يمرّان من نفس الدالّة — تطبيعُ طرفٍ واحد أسوأ من لا تطبيع: يفشل بصمتٍ ويبدو
أنه يعمل. ومرآتُها بالقاعدة `search_norm` (0150): **الأرقامُ الشرقية تُترجم قبل
مسح التشكيل** لأن مدى التشكيل U+064B–U+0670 يضمّها؛ عكسُ الترتيب يمحو «٧٧٠٩٩»
كلَّه فيطابق البحثُ كلَّ فاتورة. أمسكه فحصُ التطابق لأنه يستعمل `searchable`
الأصلية لا نسخةً — فأيُّ تطبيعٍ جديد بالقاعدة يُفحص بنفس الطريقة.

---

**قراءةٌ كتبت بأرض غيرها (0153).** ثلاثةَ عشرَ صفَّ إعداداتٍ (سلالة، دواء،
١١ مؤشّراً حيوياً) هبطت بعيادة زبونٍ بعد **ثانيتين** من دخول المشغّل، بلا أن
يلمس أحدٌ شيئاً. السببُ فرعٌ قديم بالمُرطِّبات: «إن كان الجدولُ السحابيّ فارغاً
فارفعْ ما بهذا الجهاز» — صحيحٌ ما دام المتصفّحُ والخادمُ يقصدان العيادةَ نفسها،
وكاذبٌ حين يختلفان (المشغّلُ داخل، أو لحظةٌ قبل استقرار الجلسة): المفتاحُ
المحلّيّ باسم عيادةٍ و`auth_clinic()` عيادةٌ أخرى. فكلُّ كتابةٍ داخل مُرطِّب
تمرّ من `seedOwnClinic` (تسأل `my_workspace()`: العيادتان تتّفقان؟ والمشغّلُ
ليس داخلاً؟)، و`seed-guard` يفشّل البناءَ على أي كتابةٍ عارية. **القياسُ سبق
التشخيص**: سجلُّ التدقيق حسمها بالثانية، لا التخمين.

**الماسحُ سليم والرمزُ يصل بلا رأسه.** «أيَّ باركود نمسحه يقول ما ينعرف
والمادّة بالمخزن» — والخادمُ يرجع المنتج والباركودُ نظيف. سجلُّ طلبات الخادم
حسمها: كلُّ مسحةٍ فاشلة ١١ أو ١٢ رقماً وكلُّ ناجحة ١٣، ونفسُ العلبة تنجح بعد
ثوانٍ. المجمِّعُ كان يقيس الفجوةَ بين الضغطات لحظةَ **معالجتها** (`Date.now()`)،
وأوّلُ رقمٍ يدخل حقلَ البحث فيُصفّي مئاتِ المواد ويرسمها، فتصل الضغطةُ الثانية
«بطيئة» ويُمسح ما قبلها. الدفاع: `scanBuffer.ts` (زمنُ الحدث لا زمنُ معالجته،
والحكمُ على الدفعة كلّها)، والتصفيةُ على `useDeferredValue`، و`matchTruncatedCode`
يلقى ذيلَ الرمز إن كان لمنتجٍ واحد. **تشخيصٌ أوّل («مدخول برقم رفّ») فسّر بعضَ
الفشل لا كلَّه — القياسُ صحّحه.** وذيلُ الحكاية: نافذةُ «باركود ما ينعرف —
اربطه بمنتج قائم» كانت تربط بضغطةٍ وحدة وأوّلُ صفٍّ ثابت، فانربط باركودان
أجنبيان بـ«amino acide» وصارت مسحتُهما تبيعها. **أُلغيت النافذةُ بقرار المالك**:
المقيسُ أن ما يصلها رموزُ موادٍّ غيرِ مُدخَلةٍ أصلاً، فالربطُ فيها ضررٌ لا نفع.
المجهولُ يُقال بالرمز كما وصل، والرموزُ الإضافية القائمة (`alt_codes`) تُرى
بقائمة المخزون وتُفكّ من نموذج التعديل — لا تُعِد نافذةَ ربطٍ بلا كلمته.

**سياسةٌ تقرأ جدولَها تُسقط كلَّ تحديثٍ عليه (0159 ثم 0162).** `with check` فيه
استعلامٌ فرعيّ على الجدول المحميّ يرفضه بوستغريس عند **إعادة كتابة** الاستعلام
(`42P17`) — لكلّ طلبٍ بدور `authenticated`، مهما كان الدور. أربعُ سياساتٍ وقعت
فيها (التوصيل، الفواتير، الإعدادات، الملفّ) فرجعت 500 على حفظِ إعداداتٍ وتعديلِ
فاتورة. **تجميدُ الأعمدة يكون بمحفّزٍ** يقارن OLD بـNEW ويرفض بـ`hint` عربيّ،
والسياسةُ شرطُ ملكيّةٍ وحده. والمحفّزُ يحرس `current_user = 'authenticated'` فقط
فلا يشدّ أكثر من السياسة (دوالُّ المالك تمرّ)، ولهذا هو invoker لا definer.
يمنعُها الآن `db-guard` (policy-self-ref) و`verify_no_self_ref_policies()`.
**والحزمةُ لا ترى هذا الصنف** لأنها superuser يتجاوز RLS — فحصُه يمرّ من
`_rls_try` بدور `authenticated` (run.sh).

**الكتابةُ تُسمَع: `maybe()` للقراءة لا للكتابة.** تطبع خطأ الخادم بالكونسول
وترجع «لا صفّ»، ومثلُها تحديثٌ ردّته السياسةُ: صفرُ صفوفٍ **بلا خطأ**. فتُغلق
النافذةُ ويُقال «تمّ» ولا شيء حُفظ — والصمتُ يُصدَّق. كتاباتُ المخزون والبيع
تمرّ من `updated<T>()` (ترمي على الخطأ وعلى صفرِ الصفوف)، وكلُّ مواضع ندائها
داخل `try/catch` يعرض رسالة. `updatePet`/`updateAppointment` بعدها على القديم.

**وقائمةٌ فارغةٌ عن خطأ تقلب معنى المال.** `listCouriers` كانت تبلع خطأها، فكلُّ
طلبِ شركةٍ يبدو طلبَ سائقٍ وضغطةُ «استلمنا الفلوس» تسجّل تحصيلاً لم يحصل. القوائمُ
التي يُبنى عليها قرارُ مالٍ ترمي، والشاشةُ تقول «أعد المحاولة» بدل لوحةٍ كاذبة.

**المتبقّي ليس القيمة.** `courier_settle` لا تختم `collected_at` إلا بسدادٍ كامل،
فكلُّ طلبٍ محصَّلٍ متبقّيه صفر — وحسابُ «انحصّل» و«قيمة البضاعة» من `dueOf` كان
يمحو من الكشف كلَّ ما حُصِّل. القيمةُ من الفاتورة كاملةً، والمحصَّلُ من المدفوع.
وقالبُ الفحص كان يخفيها بحالةٍ لا تصنعها القاعدة — **فالقالبُ يُقاس على ما تُنتجه
القاعدة فعلاً، لا على ما يُسهّل كتابة الفحص.**

**الطابورُ يعيش بالجهاز، والجهازُ يتبدّل عليه أهلُه.** عملياتُ الصادر بلا ختمِ
هويّة كانت تُرفع بهويّة الداخل بعدها — فمرتجعُ عيادةٍ ومصروفُها يهبطان بدفتر
عيادةٍ أخرى. لكلّ عمليةٍ الآن `who` (المستخدم + العيادة)، ولا تُرفع إلا بهويّته.

**مفتاحٌ مكرّرٌ بملفّ الترجمة يُطمس بصمت** — `JSON.parse` يأخذ الأخير، وعددُ
المفاتيح لا يتغيّر فلا يمسكه فحصُ التكافؤ. يمسكه `i18n-guard` الآن بمسحِ النصّ الخام.

**مشغّلُ المنصّة يدخل العيادة بهويّته (0151).** `auth_clinic()` تقرأ
`platform_sessions` **أوّلاً** بشرط `is_platform_admin()` (بريدُ الرمز) —
فصفُّ الجلسة وحده لا يمنح شيئاً، وكلُّ سياسةٍ ودالّةٍ وقيمةٍ افتراضية تعمل
كأنه من كادر العيادة بدور مدير بلا مسارٍ ثانٍ. **بالاتفاق مع العيادات لا أثرَ
للدخول عندها**: لا سطرَ بسجلّ حركاتها، وسجلُّ الجلسات `platform_session_log`
للمشغّل وحده — لا تُضِف أثراً بلا كلمة المالك. لا تُعِد تعريف
`auth_clinic`/`auth_role_base`/`auth_role` بلا نقل هذا الفرع — تعريفٌ لاحق
بلا الفرع يُسقط اللوحةَ بصمت.

## ٤) البنية بسطر

- `src/lib/repo.ts` — وكيلٌ موحَّد: تجريبيّ (localStorage) / سحابيّ (Supabase).
  `READ_ONLY_ALLOWED` يحكم ما يُسمح باشتراكٍ منتهٍ. **العقدةُ الحاكمة**: خللٌ
  فيها وصل ٢١ موضعاً بـ١٩ نوعاً.
- `src/lib/payrollDemo.ts` — **يفرض حرّاس الخادم نفسها**. حارسٌ لا يوجد هنا
  حارسٌ لم يُفحص، لأن فحوص المنطق تجري على النسخة التجريبية.
- `supabase/migrations/` — مرقَّمة تسلسلياً، وكلُّ هجرة تُعاد بلا أثرٍ ثانٍ،
  وتُضاف لحزمة `supabase/tests/run.sh`. **هجرةٌ لا تنزل بالحزمة هجرةٌ غير مفحوصة.**

---

## ٥) النشر

| الطبقة | كيف |
|---|---|
| الواجهة | دفعة إلى `main` ⇒ Vercel يبني ⇒ `doctorvet.vet` |
| القاعدة | هجرة تُطبَّق على مشروع Supabase `brydcepzrckstddcuuou` |

**الترتيب مهمّ**: القاعدة أولاً ثم الواجهة. الهجراتُ إضافيةٌ فتحتملها النسخةُ
القديمة؛ أما واجهةٌ تسبق هجرتَها فتنادي دالّةً غير موجودة.

---

## ٦) طريقة الشغل الي أثبتت نفعها

- **قِس قبل ما تشخّص.** خمسةُ تشخيصاتٍ لمشكلة «المنتج يختفي» سقطت أمام
  القياس: سجلُّ التدقيق وسجلُّ طلبات الخادم يحسمان ما لا يحسمه التخمين.
- **اكتب الفحص مع الإصلاح.** إصلاحٌ بلا فحصٍ إصلاحٌ يرجع.
- **رسالةُ الدفعة تشرح الجذر لا التغيير.** هي سجلُّ المشروع الحقيقي، وهي ما
  يُقرأ بعد ستّة أشهر.
- **صحّح نفسك بصوتٍ عالٍ.** ادّعاءٌ غلط أسوأ من «ما أعرف».

---

## ٧) أدوات

`graphify` (اختيارية، يعيد تنزيلها خطّافُ الجلسة): خريطةُ بنيةٍ للمشروع
بـ`graphify-out/`. تنفع **بين الملفّات** (`graphify path "A" "B"`)، وضعيفةٌ
داخل الملفّ الواحد — جرِّبت على `allPages` فرجّعت رابطةً واحدة من ٢١.
**مساعدةُ توجّهٍ لا بديلٌ عن قراءة الشِفرة.** والخريطةُ تبيَخ: حدِّثها بـ
`graphify update .` بعد التعديل، وإلا فهي خريطةُ ماضٍ.
