-- ============================================================================
-- ٠١٤٧ — خياران لشاشة البيع المتطوّرة، على مستوى العيادة
--
-- الشكوى: «المنتجات مربّعات كبيرة تاكل الشاشة على حساب السلة»، و«صندوق الزبون
-- ينطوي وأنا أريده مفتوحاً». شاشاتُ بعض العيادات صغيرة، فكلُّ بكسلٍ يُحسب.
--   • pos_compact       — المنتجاتُ والخدماتُ سطورٌ لا مربّعات، والسلّة أعرض.
--   • pos_customer_open — صندوقُ الزبون يبقى مفتوحاً بلا طيّ.
-- كلاهما يخصّ الشاشة المتطوّرة (pos_v2) فقط، ويُقرأ عبر clinic_prefs كبقية
-- الخيارات. إضافيّ ويُعاد بلا أثرٍ ثانٍ.
-- ============================================================================

alter table public.clinic_prefs add column if not exists pos_compact       boolean not null default false;
alter table public.clinic_prefs add column if not exists pos_customer_open boolean not null default false;
