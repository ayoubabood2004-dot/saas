-- 0120: كلفة الخدمة الاختيارية
--
-- عمود cost على clinic_services: كلفة الخدمة (مواد مستهلكة، مختبر خارجي…).
-- فارغ = بلا كلفة (السلوك القديم). عند البيع تدخل ككلفة سطر الفاتورة
-- فيصير ربح الخدمة بالتقارير = السعر − الكلفة بدل 100% دائماً.

alter table public.clinic_services add column if not exists cost numeric;
