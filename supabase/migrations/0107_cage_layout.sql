-- ============================================================================
-- 0107 — تخطيط الأقفاص (خريطة الأقفاص المرئية)
--
-- العيادة ترسم غرفها وأقفاصها مرة واحدة، فتصير شاشة «خريطة الأقفاص» تعرض
-- المنشأة كما هي على الأرض: كل قفص ببطاقته، والحيوان الراقد داخله، والسحب
-- والإفلات ينقله — والنقلة تُسجَّل تلقائياً بجدول تنقلات الحيوان عبر مشغّل
-- هجرة 0070 الموجود أصلاً (cage_changed)؛ هذه الهجرة لا تحتاج أي منطق جديد
-- على القاعدة، فقط مكان تخزين التخطيط.
--
-- التخزين نص JSON بعمود clinic_prefs — نفس نمط delivery_zones وqty_promos:
-- بيانات تهيئة صغيرة تخص العيادة، تُقرأ دفعة واحدة مع بقية التفضيلات.
-- ============================================================================
alter table clinic_prefs add column if not exists cage_layout text;
