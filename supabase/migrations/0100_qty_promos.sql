-- ============================================================================
-- 0100 · عروض الكمية — «كل N قطع خصم X»
--
-- الدكتور يعرّف من الإعدادات قواعد خصم كمية: على منتج محدد أو أي منتج،
-- كل N قطع من نفس السطر تعطي خصماً مقداره X. الخصم لا يُطبَّق تلقائياً:
-- بشاشة البيع يظهر زر أحمر صغير بجانب السطر الذي وصل للعدد، وضغطه يطبّق
-- الخصم (وضغطه ثانية يلغيه) — القرار دائماً بيد الكاشير.
--
-- القواعد تُخزَّن كنص JSON داخل clinic_prefs — نفس نمط مناطق التوصيل 0099
-- (مرآة محلية + مزامنة معلّقة قبل الهجرة، فلا تضيع قاعدة على قاعدة بيانات
-- لم تُهاجَر بعد).
-- ============================================================================

alter table clinic_prefs add column if not exists qty_promos text;
