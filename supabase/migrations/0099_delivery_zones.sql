-- ============================================================================
-- 0099 · مناطق التوصيل — الدكتور يحدد لوين طالع الطلب
--
-- العيادة تعرّف مناطقها (اسم + أجرة توصيل اختيارية) مرة وحدة بالإعدادات،
-- وعند البيع بالتوصيل يختار الكاشير المنطقة فتنملي الأجرة تلقائياً وتنحفظ
-- المنطقة على الطلب — تظهر بلوحة التوصيل وبوصل السائق المطبوع.
--
--   · clinic_prefs.delivery_zones: قائمة المناطق كنص JSON — نفس نمط بقية
--     تفضيلات العيادة (مرآة محلية + مزامنة معلّقة قبل الهجرة).
--   · delivery_orders.zone: اسم المنطقة المختارة على الطلب نفسه.
-- ============================================================================

alter table clinic_prefs add column if not exists delivery_zones text;

alter table delivery_orders add column if not exists zone text;
